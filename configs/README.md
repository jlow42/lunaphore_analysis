# SPARC Configuration Presets

This directory will contain YAML files describing reproducible pipeline configurations (e.g., fast preview vs publication grade).

See `../docs/system_overview.md` for configuration conventions and required metadata.
